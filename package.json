{
    "name": "mesa-mestre",
    "private": true,
    "packageManager": "bun@1.3.9",
    "scripts": {
        "dev": "turbo dev",
        "db:push": "bun --cwd apps/api drizzle-kit push",
        "db:reset": "bun --cwd apps/api drizzle-kit push --force"
    },
    "devDependencies": {
        "turbo": "^2.8.9"
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "dependencies": {
        "tw-animate-css": "^1.4.0"
    }
}